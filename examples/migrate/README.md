# Pebble Migration Example

`migrate` loads existing markov chain json files and migrates it to the new pebble storage backend.

## Usage
```
> go build -o migrate .
> ./migrate path/to/json/files)
```
